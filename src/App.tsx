import React from 'react';

export const App = () => {
    const str = 'stingstring';

    if (str == 'stingstring') {
        console.log('kek')
        console.log('kek');
    }

    return <button type='button'>FB</button>;
};
